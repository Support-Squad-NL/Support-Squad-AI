---
summary: "CLI reference for `supportsquadai reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
title: "reset"
---

# `supportsquadai reset`

Reset local config/state (keeps the CLI installed).

```bash
supportsquadai reset
supportsquadai reset --dry-run
supportsquadai reset --scope config+creds+sessions --yes --non-interactive
```
