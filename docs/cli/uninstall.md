---
summary: "CLI reference for `supportsquadai uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `supportsquadai uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
supportsquadai uninstall
supportsquadai uninstall --all --yes
supportsquadai uninstall --dry-run
```
