---
summary: "CLI reference for `supportsquadai clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `supportsquadai clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `supportsquadai clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `supportsquadai clawbot qr` (same behavior as [`supportsquadai qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `supportsquadai clawbot qr` -> `supportsquadai qr`
